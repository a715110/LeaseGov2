/**
 * Classification Decision Gate
 * Screen key: reassessment-classification
 * Route: /reassessment/cases/:id/classify
 * Feature cluster: FC — see Screen Registry Specification V2 Part 6
 *
 * STATUS: Stub — not yet implemented
 * Build session: TBD
 */

export default function ReassessmentClassification() {
  return (
    <div className="p-8">
      <h1 className="text-2xl font-semibold text-foreground">Classification Decision Gate</h1>
      <p className="mt-2 text-muted-foreground text-sm">
        Screen key: <code className="font-mono">reassessment-classification</code>
      </p>
      <p className="mt-1 text-muted-foreground text-sm">Route: /reassessment/cases/:id/classify</p>
    </div>
  )
}

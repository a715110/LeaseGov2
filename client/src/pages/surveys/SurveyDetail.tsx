/**
 * Survey Detail
 * Screen key: surveydetail
 * // TODO: Implement screen UI
 */
import StubPage from "@/components/dev/StubPage";

export default function SurveyDetailPage() {
  return (
    <StubPage
      screenKey="surveydetail"
      title="Survey Detail"
      description="Survey detail — questions and responses"
    />
  );
}

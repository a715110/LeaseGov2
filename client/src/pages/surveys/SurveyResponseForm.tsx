/**
 * Survey Response Form
 * Screen key: surveyresponseform
 * // TODO: Implement screen UI
 */
import StubPage from "@/components/dev/StubPage";

export default function SurveyResponseFormPage() {
  return (
    <StubPage
      screenKey="surveyresponseform"
      title="Survey Response Form"
      description="Survey response form — for counterparty respondents"
    />
  );
}

/**
 * Add Document to Record
 * Screen key: records-add-document
 * Route: /records/:id/add-document
 * Feature cluster: FC — see Screen Registry Specification V2 Part 6
 *
 * STATUS: Stub — not yet implemented
 * Build session: TBD
 */

export default function RecordsAddDocument() {
  return (
    <div className="p-8">
      <h1 className="text-2xl font-semibold text-foreground">Add Document to Record</h1>
      <p className="mt-2 text-muted-foreground text-sm">
        Screen key: <code className="font-mono">records-add-document</code>
      </p>
      <p className="mt-1 text-muted-foreground text-sm">Route: /records/:id/add-document</p>
    </div>
  )
}

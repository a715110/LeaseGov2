/**
 * Record Search and List
 * Screen key: records-search
 * Route: /records
 * Feature cluster: FC — see Screen Registry Specification V2 Part 6
 *
 * STATUS: Stub — not yet implemented
 * Build session: TBD
 */

export default function RecordsSearch() {
  return (
    <div className="p-8">
      <h1 className="text-2xl font-semibold text-foreground">Record Search and List</h1>
      <p className="mt-2 text-muted-foreground text-sm">
        Screen key: <code className="font-mono">records-search</code>
      </p>
      <p className="mt-1 text-muted-foreground text-sm">Route: /records</p>
    </div>
  )
}

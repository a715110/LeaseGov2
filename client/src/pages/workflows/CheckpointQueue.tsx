/**
 * Checkpoint Queue
 * Screen key: checkpointqueue
 * // TODO: Implement screen UI
 */
import StubPage from "@/components/dev/StubPage";

export default function CheckpointQueuePage() {
  return (
    <StubPage
      screenKey="checkpointqueue"
      title="Checkpoint Queue"
      description="Human checkpoint queue — all pending approvals"
    />
  );
}

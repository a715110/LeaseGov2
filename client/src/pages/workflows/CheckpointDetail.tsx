/**
 * Checkpoint Detail
 * Screen key: checkpointdetail
 * // TODO: Implement screen UI
 */
import StubPage from "@/components/dev/StubPage";

export default function CheckpointDetailPage() {
  return (
    <StubPage
      screenKey="checkpointdetail"
      title="Checkpoint Detail"
      description="Checkpoint detail — agent data + decision form"
    />
  );
}
